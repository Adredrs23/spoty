scalar JSON
scalar Date

type Album {
  id: String!
  name: String!
  songs: [Song]
}

input CreateAlbumInput {
  name: String!
  songs: [CreateSongInput]
}

type Query {
  downloadFile: String
  uploadFile: String
}
